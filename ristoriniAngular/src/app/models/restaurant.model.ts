export interface Restaurant {
  id: string;
  name: string;
  description?: string;
  address?: string;
  locality?: string;
  phone?: string;
  website?: string;
  openingHours?: string;
  images?: string[];
}
